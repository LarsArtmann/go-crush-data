# Status Report — TODO List Execution: T8, T9, T10, T5

**Session:** 2026-08-16 ~04:20–04:57 CEST · start HEAD `383bd92`.
**Trigger:** resume directive — execute the harvested TODO_LIST items one at
a time, verify each, keep going until done.
**Headline:** all four actionable items are done and gate-verified: T8
(part-level corruption tolerance), T9 (documented stats caps + pin test),
T10 (AgentGraph N+1 → single recursive CTE, ~4.6× faster on the bench), and
T5 (resolved WITHOUT filing: the false positive is upstream-tracked as
securego/gosec#1712; contributed our verified data point as a comment
instead). T1–T4, T6, T7 were checked and are still blocked on their
external dependencies.

Verification: full canonical gate green in-session
(`go build && go vet && go test -race -shuffle=on && nix run .#lint &&
nix flake check && actionlint && check-doc-links.sh` → GATE_OK), plus the
real-DB rule: `TestSessionsOnRealDatabase` green against
`/home/lars/projects/go-crush-data/.crush` and a throwaway consumer
exercised the new AgentGraph + Messages paths over 9 roots / 12 graph
nodes / 2086 messages / 5299 parts of live data.

## a) FULLY DONE (verified in-session)

1. **TODO_LIST T8 — part-level corruption no longer nukes a message**
   (`parts.go`, `messages.go`, `types.go`):
   - `DecodeParts` stays the strict all-or-nothing public decoder (the
     "strict path" the item asked to expose already existed); it now
     delegates to `decodeParts(raw, strict)`.
   - `Messages` decodes tolerantly: one malformed entry degrades to
     `UnknownPart{Type, Data}` (raw payload preserved = the signal the old
     behavior lacked) while well-formed siblings survive; input that is not
     parseable as a whole still yields nil Parts.
   - Tests: `TestDecodePartsTolerantKeepsSiblings` (strict errors where
     tolerant keeps 3 parts) and `TestMessagesKeepsSiblingsAroundCorruptedPart`
     — the required corrupted-`tool_call`-among-valid-parts integration
     test. Docs on `Messages`, `Message.Parts`, and `UnknownPart` updated.
2. **TODO_LIST T9 — stats caps documented and pinned** (`types.go`,
   `stats_test.go`): field docs on `Stats.SessionTitles` (top 20 by message
   count) and `Stats.ModelBreakdown` (top 20 by cost) state the silent
   LIMIT 20; `TestStatsCapsAt20` seeds 25 sessions/models and asserts
   exactly 20 entries each, so the documented number cannot drift from the
   parity SQL. Chose documentation over `StatsFilter` configurability — no
   demonstrated need, and the SQL is a parity contract (documented
   tradeoff, not scope creep).
3. **TODO_LIST T10 — AgentGraph N+1 removed** (`agents.go`):
   - One `WITH RECURSIVE` query fetches the whole subtree (anchor = direct
     children, recursion gated `subtree.depth < maxAgentDepth+1` so a
     depth-65 chain is still VISIBLE and errors as
     `ErrGraphDepthExceeded` instead of silently truncating at the SQL
     boundary); preorder is rebuilt in memory (`childIndex` groups +
     `sortByCreated` siblings, recursive append).
   - All seven existing AgentGraph tests pass unchanged (preorder, sibling
     created-vs-updated order, depth cap, 100-wide fan-out, legacy flat
     fallback, missing root).
   - `BenchmarkAgentGraph` (100 children): 1.58ms → 0.34ms/op, 299KB →
     172KB/op, 5409 → 1315 allocs/op vs the committed baseline.
   - Side effects: `costColumn` constant extracted into `schema.go` (goconst
     demanded it once the third site appeared); agents.go row updated in
     AGENTS architecture table; accepted-clones note extended.
4. **TODO_LIST T5 — gosec G701 upstream: duplicate avoided, data point
   contributed**:
   - Verify-before-filing gates run: built minimal repros (free-function
     and method-receiver shapes); standalone gosec v2.26.1 and v2.28.0
     never fire G7xx on this tree; clean-cache golangci-lint v2.12.2 (which
     pins gosec v2.26.1) also never fires, linux and GOOS=windows.
   - Gate 5 (does it already exist?) HIT: securego/gosec#1711 (closed) and
     **#1712 (open)** — G7xx taint findings are nondeterministic across
     runs (`maxCallerEdges=32` + unordered CHA edges + shared visited-map).
     Our repo saw both directions: the G701 firing (recorded in the
     lint-config rationale) AND run 31920446013 where the finding SLEPT and
     nolintlint broke the windows leg ("directive unused").
   - No new issue filed (would be a duplicate). Posted the verified data
     point — FP-when-it-fires + sleeping-breaks-nolintlint + never
     reproduces locally — as
     [issuecomment-5305416042](https://github.com/securego/gosec/issues/1712#issuecomment-5305416042).
   - The config-level exclusion in `.golangci.yml` stays (sufficient per the
     item itself); AGENTS.md gotcha now links the upstream tracker.

## b) CHECKED, STILL BLOCKED (external)

- **T1** Renovate: app install needs the GitHub UI (user action);
  `renovate.json` present and validated previously.
- **T2** Nightly fuzz: no `fuzz.yml` runs exist yet (only CI + Benchmark
  trend have run; first 03:17 UTC run pending). Nothing to observe.
- **T3** flake-update: zero PRs of any kind on the repo so far.
- **T4** pkg.go.dev: page still renders v0.1.1 (fetched 2026-08-16 ~04:30
  CEST), still showing the old `Session.Todos string` doc; proxy serves
  v0.2.1. Wait for the crawl.
- **T6/T7** depend on T2/T1 respectively.

## c) Lessons recorded in AGENTS.md

- golangci-lint cache served ANOTHER tree's findings when linting a copy of
  the repo (cache-hit G701 from the original, absolute paths and all;
  clean-cache rerun = 0). `cache clean` before claiming reproducibility.
- gosec taint nondeterminism now anchored to upstream #1712 + our comment.
- Stale "covered by //nolint:gosec" bullet removed (contradicted the
  config-level-exclusions rule below it).

## d) Docs updated

TODO_LIST (T5/T8/T9/T10 deleted — IDs retired), CHANGELOG `[Unreleased]`
(Fixed: tolerant part decode; Changed: single-CTE AgentGraph), FEATURES
(tolerant-decode, agent-graph, model-breakdown rows refreshed with current
citations), AGENTS (table rows, clones note, two gotchas).
